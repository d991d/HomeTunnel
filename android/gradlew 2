#!/bin/sh
# Copyright (c) 2026 d991d. All rights reserved.
# Gradle wrapper shell script — generated by Gradle, maintained by d991d

##############################################################################
# Gradle start-up script for POSIX generated by the Gradle 'init' task.
##############################################################################
# Attempt to set APP_HOME
DIRNAME="$(dirname "$0")"
APP_HOME="$(cd "$DIRNAME" && pwd)"
APP_NAME="Gradle"
APP_BASE_NAME="$(basename "$0")"

# Add default JVM options
DEFAULT_JVM_OPTS='"-Xmx64m" "-Xms64m"'

# Use the maximum available memory when JVM options are not configured
if [ -n "$JAVA_OPTS" ]; then
  JVM_OPTS="$JAVA_OPTS"
else
  JVM_OPTS="$DEFAULT_JVM_OPTS"
fi

# OS specific support
case "$(uname -s)" in
  CYGWIN*|MINGW*|MSYS*|Windows_NT)
    APP_HOME="$(cygpath --path --mixed "$APP_HOME")"
    ;;
esac

CLASSPATH="$APP_HOME/gradle/wrapper/gradle-wrapper.jar"

# Determine the Java command to use to start the JVM
if [ -n "$JAVA_HOME" ] ; then
  if [ -x "$JAVA_HOME/jre/sh/java" ] ; then
    JAVACMD="$JAVA_HOME/jre/sh/java"
  else
    JAVACMD="$JAVA_HOME/bin/java"
  fi
  if [ ! -x "$JAVACMD" ] ; then
    die "ERROR: JAVA_HOME is set to an invalid directory: $JAVA_HOME"
  fi
else
  JAVACMD="java"
  if ! command -v java >/dev/null 2>&1; then
    echo "ERROR: JAVA_HOME is not set and no 'java' command could be found."
    echo "Please set the JAVA_HOME variable in your environment to match the"
    echo "location of your Java installation."
    exit 1
  fi
fi

exec "$JAVACMD" \
  $JVM_OPTS \
  -classpath "$CLASSPATH" \
  org.gradle.wrapper.GradleWrapperMain \
  "$@"
